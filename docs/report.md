## Используемые модели

Llm - основная (отвечает на вопрос с обогащенным контекстом)
```
LOCAL_MODEL=google/gemma-4-e2b
```

Llm - судья 
```
JUDGE_MODEL=google/gemma-4-26b-a4b-qat
```

Thinking - надо руками отключать для обеих моделей в LmStudio руками (через запрос нельзя). 
```
LOCAL_REASONING_EFFORT= - не ставим иначе отклоючение thinking не поможет
```

Настройки gemma моделей:
```
Temperature: 1
Top K Sampling: 64
Repeat Penalty: 1.1
Top P Sampling: 0.95
```
Температура высоковата. Не знаю в чем причина, но при низких температурах больше вероятность что модель зациклится при ответе. Поставил 1 чтобы не рисковать (+ Repeat Penalty: 1.1).
(Зацикливание не зависит от контейнера для инфиренса: LlmStudio/Ollama/Omlx)

Эмбедер
```
EMBEDDING_MODEL=BAAI/bge-m3
```

Реранкер
```
RERANK_MODEL=BAAI/bge-reranker-v2-m3
```

## Метрики

Описание метрик тут: TODO ссылка на src/eval/README.md 

## Бейзлайн

Заведены 20 контрольных вопросов к отчётам с эталонными страницами и подстроками, скрипты
прогона и метрики — чтобы сравнивать базовый RAG с Advanced RAG на одном наборе.

Запуск
```
uv run python -m eval.run full --judge --name baseline
```

Снимок базового RAG с метриками и настройками, при которых снят:
[docs/eval/baseline.md](eval/baseline.md.bak).

## Фильтрация по Metadata

К отчету 2024 добавлены метаданные: year: 2024
К отчету 2025 добавлены метаданные: year: 2025

Запуск
```
uv run python -m eval.run full --judge --filters --name filters
```
Результат с метриками: TODO
У скольких вопросов были теги: TODO
Как повлияло: TODO

Улучшения есть местами - возможно в пределах погрешности, для точности проверить на конкретных вопросах - отдельно

TODO пример запуска на одном вопросе без тегов и с тегом года

## Реранкинг

```
Модель: BAAI/bge-reranker-v2-m3 
CANDIDATE_TOP_K=20
```

Запуск
```
uv run python -m eval.run full --judge --rerank --name rerank
```
Результат с метриками: TODO
Как повлияло: 
- где-то ухудшился в сравнении с baseline
- на уровне шума - после поправки размеров макс чанка реранкера - вроде метрики все чуть подросли - шум или нет?

## Гибридный поиск с реранкингом

Запуск
```
uv run python -m eval.run full --judge --hybrid  --rerank --name hybrid
```
Результат с метриками: TODO

Как повлияло:

## Гибридный поиск с реранкингом и разным MMR

Запуск
```
MMR_THRESHOLD=0 uv run python -m eval.run full --judge --filters --hybrid --rerank --name hybrid-mmr00
MMR_THRESHOLD=0.3 uv run python -m eval.run full --judge --filters --hybrid --rerank --name hybrid-mmr03
MMR_THRESHOLD=0.6 uv run python -m eval.run full --judge --filters --hybrid --rerank --name hybrid-mmr06
```
Результат с метриками: TODO

Как повлияло:

# Сводная таблица: baseline/hybrid+reranking/hybrid+reranking+mmr 0.3


# Ответы на вопросы задания

> Кусок кода, демонстрирующий, как именно вы извлекаете и прикрепляете метаданные (номера страниц) при парсинге PDF.

```
TODO
```

> Текст вашего итогового системного промпта.
 
```
Ты — ассистент по годовым отчётам компании.

Правила:
1. Отвечай только на основе контекста ниже.
2. Если ответа в контексте нет, напиши ровно: "Я не знаю".
3. Каждый факт подтверждай ссылкой вида [file_name, стр. page_label].
4. Номер страницы бери только из поля page_label, не из текста фрагмента.
5. Отвечай на языке вопроса.

Контекст:
---------------------
{context_str}
---------------------

Вопрос: {query_str}
Ответ: 
```

> Логи тестирования: 5 ваших тестовых вопросов и полные ответы ассистента с кликабельными или явно выделенными ссылками на источники.
 
```
TODO 5 показательных вопросов и ответов для hybrid+reranking
```

> Сделайте вывод: насколько успешно удалось побороть галлюцинации LLM? 

```
Не видел такого
```

> В чем главная сложность извлечения точных номеров страниц из PDF-файлов (например, сбивается ли нумерация из-за оглавления)?

```
Сложность написать парсер и вычленить реальные номера страниц из pdf.
Если нужны реальные страницы (проставленные в документе) а не физические, то в зависимости от pdf они могут стоять на разных местах, а также pdf могут содержать как обячные A4 страницы, так и страницы разворота.
```

> Прочие сложности

- Из коробки фреймворка ничего не работает - все равно надо писать все самому когда доходит дело до реальных документов. 5 строк кода которые продает фреймворк llamaindex для Advanced RAG работают только на игрушечных примерах.
- Pdf парсить сложнее текстового, например: с графиками разного рода, диаграммами и картинками, разными разворотами страниц. Если в базе уникальные pdf то либо терять информацию либо под каждый документ писать кастомный парсер.
- Neo4j + хелперы llamaindex для граф-RAG - не работает из коробки - не повышает качество. Пробовал в разных вариантах применения графа и по разному строил сам граф: как с LLM так и детерминировано через парсинг отчета. Максимальный результат: около baseline (+/-). Из репозитория удалил код, чтобы не отчитываться по нему (задание явно не требует). Для качественной реализации нужно быть погруженным в предметную область и знать какие типовые запросы будут приходить в RAG. Ни того ни другого у меня нет, отказался.   